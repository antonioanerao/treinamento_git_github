[project]
name = "git-github"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]

[dependency-groups]
dev = [
    "anakin-language-server>=1.23",
]
